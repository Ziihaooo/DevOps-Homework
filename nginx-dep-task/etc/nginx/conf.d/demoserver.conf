#make a simple configuration server which listen from port 80 and server name is local host
#and when access this port will print out Hello from nginx!
server {
    listen 80;
    server_name localhost;

    location / {
        return 200 "Hello from Nginx!\n";
        add_header Content-Type text/plain;
    }
}

